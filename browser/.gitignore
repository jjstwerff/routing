# Generated by build.mjs / build-standalone.mjs (loft --html → wasm, single-file app) — reproducible, not committed.
*.wasm
.web_kernel.html
standalone.html
areas.txt
buildings.txt
places.txt
streets.txt
lines.txt
pois.txt
stamp.txt

# M4 baked tile pyramid (bake_tiles.mjs) — regenerable from the whole-region dumps, not committed.
tiles/
