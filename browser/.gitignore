# M4 baked tile pyramid (bake_tiles.mjs, node) — regenerated at build time from the layer text below.
tiles/
# stray generated artifacts (not part of the canvas app)
*.wasm
stamp.txt

# The whole-region layer text (browser/*.txt) IS committed — it is the deploy source that build-site.mjs
# bakes into tiles/. Regenerate it from the Overpass dumps with client/basemap/emit_*.loft (needs loft).
