{
  "name": "routing-browser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless browser shell for the routing matcher (PLAN-APP Track 1) — fetch a whole test set, match in wasm, no server.",
  "license": "LGPL-3.0-or-later",
  "scripts": {
    "build": "node build.mjs",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.25.1",
    "@bytecodealliance/preview2-shim": "^0.19.0"
  }
}
